# Copy this to scripts/.env and fill in values. Never commit .env.
# Used by disable-lock-screens.sh and wake-fleet-displays.sh.

# Shared PIN for all fleet devices (set to empty string if no lock screen)
DEVICE_PIN=
